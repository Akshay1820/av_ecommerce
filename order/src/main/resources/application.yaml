spring:
  application:
    name: order
  datasource:
    url: jdbc:postgresql://${ORDER_DB_URL}
    username: ${ORDER_DB_USERNAME}
    password: ${ORDER_DB_PASSWORD}
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect

product:
  service:
    url: ${PRODUCT_SERVICE_URL}
user:
  service:
    url: ${USER_SERVICE_URL}

server:
  port: ${ORDER_PORT}